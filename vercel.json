{
  "buildCommand": "cd backend && npm install && cd ../frontend && npm install && npm run build && cd ..",
  "installCommand": "cd backend && npm install && cd ../frontend && npm install && cd ..",
  "env": {
    "MONGO_URL": "mongodb+srv://rudrapratap51479145518_db_user:Rps250206@rpscluster.xyta3km.mongodb.net/zerodha?appName=RpsCluster",
    "NODE_ENV": "production"
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "backend/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/frontend/build/index.html"
    }
  ]
}
